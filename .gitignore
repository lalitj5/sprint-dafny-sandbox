# local config
.env

# Python
**/__pycache__/
*.pyc

# Session logs are experiment data, not source. logs/ itself is tracked
# (via .gitkeep) because the dashboard reads it; the runs inside are not.
logs/*.jsonl
logs_archive/

*.md

# Canary hit records and per-session workdir snapshots (run data, not source)
logs/canary/
logs/workdir_*/
